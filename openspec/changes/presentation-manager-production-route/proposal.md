# 簡報管理師新增原生簡報製作交接路徑

## Why

原本的簡報管理師已能把內容整理成中繼 Markdown 與 Kimi PPT 提詞，但使用者在需要本機可編輯簡報時，缺少一個明確的輸出選擇與交接邊界，容易把粗略課綱直接送進製作工具，造成成品方向偏離。

## Scope

- 保留既有 Kimi PPT 提詞路徑。
- 在中繼 Markdown 確認後增加輸出路徑確認。
- 增加本機 `.pptx` 的 `ppt-master` 製作交接規則與檔案驗證要求。
- 維持不自動呼叫 Kimi、不自動貼上、不把不存在的檔案宣稱為完成。
